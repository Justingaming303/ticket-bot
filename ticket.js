module.exports =  
    class ticket{
        constructor(ticketNumber, ticketType, client, channelID, ticketChannel) {
            this.ticketNumber = ticketNumber;
            this.ticketType = ticketType;
            this.client = client;
            this.channelID = channelID;
            this.channel = ticketChannel
        }
  
    
    }